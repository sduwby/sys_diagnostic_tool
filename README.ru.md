# Инструмент системной диагностики / AWDMS

[简体中文](./README.zh-CN.md) | [繁體中文](./README.zh-TW.md) | [English](./README.md) | [Русский](./README.ru.md) | [Français](./README.fr.md)

Профессиональный инструмент практики набора текста, имитирующий рабочий процесс agile-разработки.  
Интегрирован с **AWDMS (Agile Workflow & Dependency Management System)** для симуляции реальной среды разработки.  
Поддерживает сборку для Windows, macOS и Linux.

## ✨ Основные функции

### 🎮 Режимы игры
- **Обычный режим** - Классическая ритм-игра
- **Ограниченное время** - 60-секундный спринт, скорость ×2
- **Режим выживания** - 5 ошибок = конец игры, экстремальный вызов
- **Zen-режим** - Практика без давления, регулируемая скорость (1.0x-5.0x)

### 🎯 Игровая механика
- Фрагменты кода на нескольких языках (JS, C++, Java, Go, Python + Пользовательские)
- Система уровней + Глобальная таблица лидеров
- Система комбо (3/5/10/20/50/100 эффектов комбо)
- Система достижений (18-уровневые сертификаты)

### 🔊 Звуковая система
- 15 процедурных звуковых эффектов (клик/комбо/промах/повышение уровня/достижение и т.д.)
- Управление громкостью + Быстрое отключение звука
- Постоянные настройки

### 🎨 Система тем
- 6 предустановленных тем (VS Code Dark, GitHub, Monokai, Dracula, Nord, Solarized)
- Редактор пользовательских тем (настройка 4 цветов)
- Поддержка CSS-переменных

### 📋 Система AWDMS (Проект "Moyu")
- **Ежедневные задачи**: 4-уровневые миссии (Junior → Principal)
- **Система предметов**: 6 основных зависимостей (Stable → Legacy)
- **Gacha-компиляция**: Розыгрыш пакетов исходного кода (успех 2%-100% + гарантия)
- **$COMMITS** Система кредитов
- **Профессиональный замаскированный UI** (стиль TODO.md / package.json / консоль AWS)

### 🔐 Функции безопасности
- Шифрование AES-256-GCM + HMAC-SHA256
- Локальное хранилище, без облачной синхронизации
- Быстрое переключение Boss-клавишей (`ESC`)

## Структура проекта (Модульная + 100% TypeScript)

```text
sys_diagnostic_tool/
├── package.json          # Конфигурация проекта
├── main.ts               # Главный процесс Electron
├── src/
│   ├── index.html        # Основной интерфейс
│   ├── app.ts            # Основная игровая логика (490 строк)
│   ├── app.css           # Система стилей CSS-переменных
│   ├── storage.ts        # Шифрование AES-256-GCM и постоянство
│   ├── achievements.ts   # Определения 18-уровневых достижений
│   ├── achievementTracker.ts  # Механизм отслеживания достижений (194 строки)
│   ├── achievementUI.ts  # Рендеринг панели достижений
│   ├── analytics.ts      # Статистика графиков Canvas
│   ├── soundEffects.ts   # Звуковой движок Web Audio API (187 строк)
│   ├──combo.ts          # Система комбо
│   ├── gameState.ts      # Сохранение/восстановление прогресса
│   ├── practiceMode.ts   # Режим практики
│   ├── challengeModes.ts # Система режимов испытаний (374 строки)
│   ├── gameConfig.ts     # Конфигурация игры (35 строк)
│   ├── cheats.ts         # Система читов (106 строк)
│   ├── leaderboard.ts    # Система таблицы лидеров (93 строки)
│   ├── terminal.ts       # Терминальная система (128 строк)
│   ├── uiHandlers.ts     # Обработчики событий UI (145 строк)
│   ├── themeSystem.ts    # Система тем (320 строк)
│   ├── dailyTasks.ts     # Ежедневные миссии AWDMS (240 строк)
│   ├── itemSystem.ts     # Система предметов AWDMS (259 строк)
│   ├── gachaSystem.ts    # Компиляция/gacha AWDMS (219 строк)
│   ├── awdmsUI.ts        # Профессиональный UI AWDMS (428 строк)
│   ├── itemEffects.ts    # Эффекты предметов AWDMS (119 строк)
│   └── icon.svg          # Иконка приложения
└── dist/                 # Вывод компиляции TypeScript
```

## Установка

Убедитесь, что у вас установлен `Node.js` (рекомендуется `v16+`), затем выполните:

```bash
npm install
```

## Разработка

Запустите приложение в режиме разработки:

```bash
npm start
```

## Сборка исполняемых файлов

### Сборка для одной платформы (архитектура по умолчанию)

**Windows (x64)**
```bash
npm run build:win
```

**macOS (x64 + arm64)**
```bash
npm run build:mac
```

**Linux (x64)**
```bash
npm run build:linux
```

### Сборка для конкретной архитектуры

**Windows**
```bash
npm run build:win:x64      # 64-битный Intel/AMD
npm run build:win:ia32     # 32-битный Intel/AMD
npm run build:win:arm64    # ARM64 (Windows on ARM)
```

**macOS**
```bash
npm run build:mac:x64        # Чипы Intel
npm run build:mac:arm64      # Apple Silicon (M1/M2)
npm run build:mac:universal  # Универсальная сборка (две архитектуры)
```

**Linux**
```bash
npm run build:linux:x64    # x64
npm run build:linux:arm64  # ARM64 (Raspberry Pi и т.д.)
```

### Сборка для всех платформ

```bash
npm run build
```

## Примечания по сборке

- Первая сборка загрузит бинарные файлы Electron (требуется сеть)
- Собранные приложения автономны, Node.js не требуется на целевой машине
- **Windows**: Установщик NSIS с поддержкой пользовательского пути и ярлыков
- **macOS**: Вывод DMG, совместимый с Intel / Apple Silicon
- **Linux**: Предоставляет пакеты AppImage и deb

## Как использовать

1. Нажимайте на падающие фрагменты кода, чтобы набрать очки
2. Разные языки имеют разные баллы и скорости, требуя фокуса и быстрых реакций
3. Наведите мышь, чтобы приостановить фрагменты для стратегического выбора времени
4. Нажмите `ESC`, чтобы активировать режим быстрой минимизации
5. Отправьте свой результат после завершения, чтобы отслеживать прогресс

## Технологический стек

- **Electron** - Кроссплатформенное десктопное приложение
- **TypeScript** - 100% типобезопасность
- **Web Audio API** - Процедурная генерация звука
- **CSS-переменные** - Динамическая система тем
- **localStorage** - Локальное постоянство данных
- **electron-builder** - Упаковка и сборка

## Примечания

- Данные таблицы лидеров хранятся в зашифрованном файле `~/.sys_diagnostic_data`
- Данные защищены шифрованием AES-256-GCM + HMAC-SHA256
- Пользовательские фрагменты и достижения также зашифрованы и хранятся локально
- Рекомендуется тестировать локально перед распространением
- Использует `src/icon.svg` для генерации кроссплатформенных иконок

## Подсказки

Код этого проекта полностью сгенерирован инструментами ИИ.  
Если есть ошибки, не вините меня 😁  
Предложения и отзывы приветствуются!

## Лицензия

MIT