# Инструмент системной диагностики

[简体中文](./README.zh-CN.md) | [繁體中文](./README.zh-TW.md) | [English](./README.md) | [Русский](./README.ru.md) | [Français](./README.fr.md)

Профессиональный анализатор точности набора текста и приложение для диагностики производительности.  
Поддерживает сборку для платформ Windows, macOS и Linux.

## Особенности

- 🎮 Практика с фрагментами кода на нескольких языках (JS, C++, Java, Go, Python)
- 🎯 Система уровней + таблица лидеров для отслеживания навыков набора
- 🔐 Локальное зашифрованное хранилище данных для конфиденциальности
- 🎭 Функция быстрой минимизации (клавиша `ESC` для мгновенного переключения окон)

## Структура проекта

```text
sys_diagnostic_tool/
├── package.json          # Конфигурация проекта
├── main.js               # Главный процесс Electron
├── src/
│   ├── index.html        # Основной интерфейс
│   ├── app.js            # Логика приложения
│   ├── app.css           # Стили
│   ├── storage.js        # Модуль зашифрованного хранения
│   ├── achievements.js   # Система достижений
│   ├── analytics.js      # Анализ данных
│   └── achievementUI.js  # UI достижений
└── dist/                 # Вывод сборки (авто-генерация)
```

## Установка

Убедитесь, что у вас установлен `Node.js` (рекомендуется `v16+`), затем выполните:

```bash
npm install
```

## Разработка

Запустите приложение в режиме разработки:

```bash
npm start
```

## Сборка исполняемых файлов

### Сборка для одной платформы (архитектура по умолчанию)

**Windows (x64)**
```bash
npm run build:win
```

**macOS (x64 + arm64)**
```bash
npm run build:mac
```

**Linux (x64)**
```bash
npm run build:linux
```

### Сборка для конкретной архитектуры

**Windows**
```bash
npm run build:win:x64      # 64-битный Intel/AMD
npm run build:win:ia32     # 32-битный Intel/AMD
npm run build:win:arm64    # ARM64 (Windows on ARM)
```

**macOS**
```bash
npm run build:mac:x64        # Чипы Intel
npm run build:mac:arm64      # Apple Silicon (M1/M2)
npm run build:mac:universal  # Универсальная сборка (две архитектуры)
```

**Linux**
```bash
npm run build:linux:x64    # x64
npm run build:linux:arm64  # ARM64 (Raspberry Pi и т.д.)
```

### Сборка для всех платформ

```bash
npm run build
```

## Примечания по сборке

- Первая сборка загрузит бинарные файлы Electron (требуется сеть)
- Собранные приложения автономны, Node.js не требуется на целевой машине
- **Windows**: Установщик NSIS с поддержкой пользовательского пути и ярлыков
- **macOS**: Вывод DMG, совместимый с Intel / Apple Silicon
- **Linux**: Предоставляет пакеты AppImage и deb

## Как использовать

1. Нажимайте на падающие фрагменты кода, чтобы набрать очки
2. Разные языки имеют разные баллы и скорости, требуя фокуса и быстрых реакций
3. Наведите мышь, чтобы приостановить фрагменты для стратегического выбора времени
4. Нажмите `ESC`, чтобы активировать режим быстрой минимизации
5. Отправьте свой результат после завершения, чтобы отслеживать прогресс

## Технологический стек

- Electron
- Vanilla JavaScript
- HTML5 / CSS3
- electron-builder

## Примечания

- Данные таблицы лидеров хранятся в зашифрованном файле `~/.sys_diagnostic_data`
- Данные защищены шифрованием AES-256-GCM + HMAC-SHA256
- Пользовательские фрагменты и достижения также зашифрованы и хранятся локально
- Рекомендуется тестировать локально перед распространением
- Использует `src/icon.svg` для генерации кроссплатформенных иконок

## Подсказки

Код этого проекта полностью сгенерирован инструментами ИИ.  
Если есть ошибки, не вините меня 😁  
Предложения и отзывы приветствуются!

## Лицензия

MIT